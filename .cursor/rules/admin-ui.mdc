---
description: jsonConfig admin UI and i18n conventions
globs: admin/**
alwaysApply: false
---

# Admin UI (jsonConfig)

## Files

- `admin/jsonConfig.json` — UI definition
- `admin/i18n/de.json`, `admin/i18n/en.json` — **manually maintained only**
- Other languages — generated by owner via `npm run translate`; do **not** edit or run translate

## jsonConfig guidelines

- End-user friendly labels; tooltips for ports / dual-API explanation
- Sensible defaults; validate required fields
- Every jsonConfig field must exist in `io-package.json` → `native` with a default

## Planned config fields (v1)

| Field | Default | Notes |
|-------|---------|--------|
| `host` | (empty) | Printer IP |
| `moonrakerPort` | `7125` | Moonraker HTTP |
| `crealityWsPort` | `9999` | Creality WebSocket |
| `pollInterval` | `5` | Seconds |
| `apiKey` | (empty) | Optional Moonraker API key |
| `enableCfs` | `true` | Create CFS channel/states |
| `enableFans` | `true` | Fan states |
| `enableControl` | `true` | Light / pause / resume / stop |

## Translations workflow

1. Add keys to `de.json` and `en.json` when changing jsonConfig
2. Keys must match; sort alphabetically
3. Do **not** run `npm run translate`
